<h1 class="home-header" style="font-size:35px"><b>Blog Posts</b></h1>
<% @posts.each do |post| %>
	<div class="well">
		<h3><%= post.title %></h3>
		<p class="blogpostdesign"><%= post.body %></p>
		<%= link_to "Read More", post_path(post), :class => 'btn 
	btn-default' %>
	</div>
<% end %>
